// send request: game -> game_id


// send request game_id, login, clip_id, first to get clips and display on right side

const sendGetRequest = function(url){
    console.log(arguments);
    console.log(url);
}

sendGetRequest("kek");